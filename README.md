# 📡 Прикладная тема

Проект посвящён автоматизации инженерных расчётов, применяемых при проектировании сетей мобильной связи.  
Он позволяет определить ключевые параметры базовой станции: радиус покрытия, нагрузку, количество необходимых станций и эффективность хэндовера.

Приложение может использоваться в учебных целях, лабораторных работах, а также как основа для профессиональных инструментов радиопланирования.

---

# 🎯 Цель проекта

Создать настольное приложение, которое:

- принимает входные параметры зоны покрытия  
- рассчитывает R₀, R, L, C, N  
- отображает результаты в удобном интерфейсе  
- использует чистую архитектуру: логика и UI разделены  
- легко расширяется и тестируется  


---

## 📂 Описание файлов

### 📦 BaseStationLibrary

**`Models/CalculationResult.cs`**  
Модель, содержащая результаты расчёта:  
`R0`, `R`, `L`, `C`, `N`.

**`Models/DistrictData.cs`**  
Модель входных данных: площадь района, покрытие, радиусы БС, коэффициент застройки и др.

**`BaseStationCalculator.cs`**  
Основной модуль расчётов. Принимает `DistrictData`, возвращает `CalculationResult`.  
Полностью независим от UI.

---

### 🖥 BaseStationWpf

**`ViewModels/MainViewModel.cs`**  
Главная ViewModel:  
обрабатывает ввод, вызывает расчёты, обновляет UI, работает с API.

**`Fonts/*.ttf`**  
Подключённый шрифт Montserrat для современного интерфейса.

**`ApiClient.cs`**  
Загрузка данных хэндовера с внешнего источника.

**`RelayCommand.cs`**  
Реализация команд MVVM (`ICommand`).

**`App.xaml` / `App.xaml.cs`**  
Точка входа приложения, глобальные ресурсы.

**`MainWindow.xaml`**  
Разметка интерфейса:  
ввод данных, результаты, стили, выравнивание.

**`MainWindow.xaml.cs`**  
Привязка ViewModel и инициализация окна.

---

### 🧪 BaseStationLibrary.Tests

**`CalculationResultTests.cs`**  
Пример юнит‑тестов для проверки корректности модели.

---

## 🚀 Итог

Проект реализует:

- инженерные расчёты параметров базовых станций  
- разделение логики и интерфейса (чистая архитектура)  
- современный UI на WPF с MVVM  
- аккуратную структуру, удобную для развития  
- возможность модульного тестирования  

Подходит для учебных работ, демонстрации архитектуры и дальнейшего расширения под реальные задачи радиопланирования.

<img width="886" height="893" alt="image" src="https://github.com/user-attachments/assets/2ec0db64-6fd9-4b73-b0e2-3f3c3928568f" />
